# docs

Tracked documentation. Personal or scratch material goes in **`docs/.local/`**, which is git-ignored — same for any file/dir containing `.local` in its name (the `*.local*` rule in the root `.gitignore`).
