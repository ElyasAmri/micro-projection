# Desktop control app (app/) Python dependencies.
# Install into a venv:  python -m venv .venv && .venv/bin/pip install -r requirements.txt
PySide6==6.11.1          # Qt bindings (pulls PySide6-Essentials + PySide6-Addons)
numpy==2.2.6             # fringe generation + reconstruction math
