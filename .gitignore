# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/

# Jupyter
.ipynb_checkpoints/

# IDE
.vscode/
.idea/

# OS
Thumbs.db
.DS_Store

# Data files (ignore large/temporary, keep small reference)
data/raw/
data/captures/
*.npy
*.npz

# Regression fixture is the versioned ground truth Stage 2 tests check
# against — keep it tracked even though *.npz is otherwise ignored.
!tests/regression_data.npz

# Byte-exact rasterizer baseline (Stage 6 B.3b-perf.0): the frozen current
# output _rasterize_triangles must reproduce. Committed for the same reason.
!tests/fixtures/rasterizer/rasterizer_baseline.npz

# B.3a reference numbers + recovered array (Stage 6 B.4.2b): the Stage 7
# hardware-phase validation target. Tolerance-checked (atol 1e-8), committed
# alongside its self-describing config JSON.
!tests/fixtures/b4_reference/b3a_reference.npz

# Copyrighted thesis chapters — kept locally for reference,
# never tracked, never pushed.
FPP Thesis/

# Conda env
*.conda