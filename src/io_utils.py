"""io_utils.py — I/O helpers for frames, calibration files, results.

Stub. Will house:
- np.savez_compressed wrappers for captured PSI stacks.
- PNG read/write for projector patterns.
- JSON / YAML save and load for `HybridGeometry` parameters and lambda_eq calibration.
"""
