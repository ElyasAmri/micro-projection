"""Calibration of the fringe-projection rig.

Modules:
    priors  -  paper-spec-derived prior calibration parameters (used as the
               solver's initial guess and as the demo for the UI scaffold).
    solve   -  calibration solver (capture a target, detect features, fit
               geometry). Pending hardware integration.
"""
