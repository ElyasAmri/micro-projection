"""synthetic_fringes.py — Forward model: phase to captured intensity frames.

Stub for Stage 2 Task 4. Will house:
- The `project()` function (Taylor approximation of Eq. 2-44) from notebook
  cell 2. Interface accepts `model={'taylor', 'exact'}` per Stage 2
  Decision 6 — only 'taylor' implemented until Stage 3.
- Phase-shifted intensity stack synthesis (A + B cos(phi + delta_k)).
"""
